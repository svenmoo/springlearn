server:
  port: 8000

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://mysql/svenmoo?useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: bsy
    password: bsy

mybatis:
  type-aliases-package: com.bsy.spring.api.entities
  mapper-locations: mappers/*.xml

logging:
  file:
    path: logs
